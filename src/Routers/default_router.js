const express = require("express");
const router = express.Router();

// Get
router.get("/api/v1/getRequest", (request, response) => {});

// Post
router.post("/api/v1/postRequest", (request, response) => {});

// Put
router.put("/api/v1/putRequest", (request, response) => {});

module.exports = router;
